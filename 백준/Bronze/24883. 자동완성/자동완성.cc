#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    char c;
    cin >> c;
    if (c == 'N' || c == 'n') cout << "Naver D2";
    else
        cout << "Naver Whale";
}